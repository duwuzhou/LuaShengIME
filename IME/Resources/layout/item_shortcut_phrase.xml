<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="6dp"
    android:paddingBottom="6dp"
    android:paddingStart="2dp"
    android:paddingEnd="2dp">

    <TextView
        android:id="@+id/tv_shortcut_phrase_content"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textColor="@color/text_primary"
        android:textSize="@dimen/text_body"
        android:maxLines="2"
        android:ellipsize="end" />

    <Button
        android:id="@+id/btn_shortcut_phrase_delete"
        android:layout_width="72dp"
        android:layout_height="36dp"
        android:text="@string/shortcut_delete"
        android:textSize="@dimen/text_caption"
        android:background="@drawable/key_function_background"
        android:textColor="@color/key_text_secondary" />

</LinearLayout>
